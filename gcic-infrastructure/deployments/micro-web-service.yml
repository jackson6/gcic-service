apiVersion: v1
kind: Service
metadata:
  name: micro-http
spec:
  type: LoadBalancer
  ports:
  - name: micro-wep
    port: 80
    targetPort: "web-port"
    protocol: TCP
  selector:
    app: micro-web